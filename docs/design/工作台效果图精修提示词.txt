Refine this RF Link UI mockup with ONE targeted visual change: make it a truly crisp flat software interface.
Preserve EXACTLY the current layout, all Chinese labels, field values, six metric values, icon positions, three connected RF devices, LNA selection, table and inspector. Preserve the one-screen design with no scrollbars. Do not add or delete interface elements.
Replace all mottled, swirly, smoky or grainy background textures, colored light patches, heavy drop shadows, glow, blur, vignette and glass-like gradients with clean solid neutral fills. Page background #F4F6F8; panels #FFFFFF; subtle borders #DDE3E8; text #263746. Keep selected LNA/row as a uniform very pale teal #E7F5F5 and active tab / primary calculation button a solid teal #087F8C. All text must be pin-sharp clean dark sans serif, with no shadows or halos. All panels and toolbar backgrounds must be flat and evenly lit. Typography and linework must look like a real vector-rendered desktop app screenshot. Maintain all content and geometry, only remove the visual texture and lighting artifacts. No explanation text, no new captions.
